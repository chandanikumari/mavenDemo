pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh `echo "Testing Jenkinsfile!"`
		`cd my-app`
		`mvn package`
		`java -cp target/my-app-1.0-SNAPSHOT.jar com.mycompany.app.App`
            }
        }
    }
}
